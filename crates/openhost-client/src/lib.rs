#![deny(unsafe_code)]
#![warn(missing_docs)]
//! openhost client library.
//!
//! Consumed by the browser extension (compiled to WASM) and by the native apps
//! (via `openhost-ffi`). Populated in M4.
