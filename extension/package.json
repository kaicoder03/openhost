{
  "name": "openhost-extension",
  "version": "0.0.2",
  "private": true,
  "license": "(Apache-2.0 OR MIT)",
  "description": "Browser extension that resolves oh://<pubkey>/ URLs over openhost WebRTC data channels.",
  "scripts": {
    "lint": "echo \"openhost-extension: JS lint lands in PR #28.4 once real service-worker code exists\"",
    "build": "./scripts/build-wasm.sh",
    "build:wasm": "./scripts/build-wasm.sh"
  }
}
